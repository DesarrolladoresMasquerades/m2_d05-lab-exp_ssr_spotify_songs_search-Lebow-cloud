

{{#each Artist}}
    <div>
       <h1>{{this.name}}</h1> 
    
       <img src ="{{this.images.[1].url}}" alt="{{this.name}}"> 
    <button><a href="/albums/{{this.id}}">View Albums</a></button>
        

    </div>
{{/each}}

