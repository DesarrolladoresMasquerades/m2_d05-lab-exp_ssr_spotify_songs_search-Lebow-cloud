


{{#each Albums}}
    <div>
       <h1>{{this.name}}</h1> 
    
       <img src ="{{this.images.[1].url}}" alt="{{this.name}}"> 
       <button><a href="/tracks/{{this.id}}">View Sample of Tracks</a></button>

    
        

    </div>
{{/each}}
